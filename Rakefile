namespace :pool do
  desc 'start pool process'
  task :start do
    run "ruby lib/twitter_streamer/pool.rb start"
  end
  desc 'stop pool process'
  task :stop do
    run "ruby lib/twitter_streamer/pool.rb stop"
  end
end